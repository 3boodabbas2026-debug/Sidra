# Original unprocessed media (too large for GitHub — the 150 MB video
# exceeds GitHub's 100 MB hard limit). The web page uses memories/ instead.
images and videos (1)/

# OS junk
Thumbs.db
desktop.ini
.DS_Store
